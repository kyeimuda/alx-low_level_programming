#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @a: first number
 * @b: second number
 * Return: the results of the addition
 */

int add(int a, int b)
{
int result;

result = a + b;
return (result);
}
