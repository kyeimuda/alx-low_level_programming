#include <stdio.h>
#include "main.h"

/**
 *main - Entry point
 *Return: Always 0 (Success)
 */

void print_alphabet_x10(void);
int main(void)

{
int ch = 'a' , j;
{
return (1);
}
for( j = '0' ; j <= 10 ; j++)
{
for( ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
putchar(j);
}
return (0);
}
