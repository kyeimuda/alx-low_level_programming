#include "main.h"

/**
 *isupper - checks a character whether the passed character is uppercase letter
 *@c: The character to print
 *Return : non zero value if true
 */
int isupper(int c)
{
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
