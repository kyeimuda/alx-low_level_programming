#include "main.h"
#include <string.h>

/**
 * _strcmp : function that compares two strings
 * @s1: a string
 * @s2: a string
 * Return: Always 0
 */

int _strcmp(char *s1, char *s2)
{
int result;
result = strcmp(s1, s2);
return (result);
}
