#include "main.h"
#include <string.h>

/**
 * _strcmp : compares two strings
 * @s1: a string
 * @s2: a string
 * Return: Always 0
 */

int _strcmp(char *s1, char *s2)
{
int result;
if (result = strcmp(s1, s2);)
{
return (result);
}
if (result = strcmp(s2, s1));
{
return (result);
}
return (result);
}
