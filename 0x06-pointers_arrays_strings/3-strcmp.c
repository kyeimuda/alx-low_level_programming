#include "main.h"
#include <string.h>

/**
 * _strcmp : compares two strings
 * @s1: a string
 * @s2: a string
 * Return: Always 0
 */

int _strcmp(char *s1, char *s2)
{
int result;
 if(strcmp(s1, s2))
   {
     result = strcmp(s1, s2);
   }
 if(strcmp(s2, s1)
   {
   result = strcmp(s2, s1);
   }
return (result);
   }
}
